# Test environment variables for E2E tests
# Authentication disabled for E2E tests (we use API mocks instead)
VITE_AUTH_ENABLED=false
VITE_AUTH_SKIP_LOGIN=false

# Empty auth providers for testing
VITE_AUTH_PROVIDERS=[]

# Base path (default for dev)
VITE_BASE_PATH=/
